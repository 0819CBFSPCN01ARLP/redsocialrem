USERS
id(pk): entero sin signo
name: string
surname: string
email: string NOT NULL
password: string NOT NULL

FRIENDS
id(pk): entero sin signo
id_user(fk): entero sin signo
id_friend(fk): entero sin signo

IMAGES
id(pk): entero sin signo
position: string
path: string
id_user(fk): entero sin signo

POSTS
id(pk): entero sin signo
title: string NOT NULL
text: string
id_image(fk): entero sin signo
id_user(fk): entero sin signo

COMMENTS
id(pk): entero sin signo
text: string NOT NULL
id_image(fk): entero sin signo
id_user(fk): entero sin signo
id_post(fk): entero sin signo

ANSWERS
id(pk): entero sin signo
answer: string NOT NULL
id_user(fk): entero sin signo
id_comment(fk): entero sin signo
